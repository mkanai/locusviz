% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_rank.R
\name{normalize_rank}
\alias{normalize_rank}
\title{Normalize scores by rank with exponential decay}
\usage{
normalize_rank(score, decay = 0.5, ties.method = "min")
}
\arguments{
\item{score}{Numeric vector of scores to be normalized}

\item{decay}{Numeric decay factor between 0 and 1 (default: 0.5).
Smaller values result in faster decay}

\item{ties.method}{Character string specifying how ties are handled.
Options: "average", "first", "last", "random", "max", "min" (default: "min")}
}
\value{
Numeric vector of normalized weights based on rank, with NA values
  receiving weight of 0
}
\description{
This function converts scores to weights based on their rank, applying an
exponential decay factor. Higher ranked scores receive exponentially
decreasing weights.
}
\examples{
# Normalize a vector of scores
scores <- c(10, 20, 15, NA, 25)
weights <- normalize_rank(scores)

# Use faster decay
weights_fast <- normalize_rank(scores, decay = 0.3)

}
